--- jarvis/reply_service_generation.py
+++ jarvis/reply_service_generation.py
@@ -214,7 +214,7 @@
                 RAGDocument(
                     content=res.get("text", "") or res.get("content", ""),
                     source=res.get("source", "unknown"),
-                    score=float(res.get("score", 0.0) or 0.0),
+                    score=float(res.get("score", res.get("similarity", 0.0)) or 0.0),
                     metadata=res,
                 )
             )
