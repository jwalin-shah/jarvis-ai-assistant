# Evals Data

Large conversation chunk exports were removed from the public repository for privacy.

To regenerate benchmark input locally, run the evaluation preparation scripts in `scripts/evaluation/`.
