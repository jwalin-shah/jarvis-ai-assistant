## 2024-05-22 - [Svelte Slide Transitions]
**Learning:** Svelte's `transition:slide` is perfect for toggleable sections but requires a block wrapper around `{#each}` loops to animate correctly.
**Action:** Always wrap `{#each}` in a `div` when animating list expansion.
